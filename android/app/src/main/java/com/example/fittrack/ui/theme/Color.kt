package com.example.fittrack.ui.theme

import androidx.compose.ui.graphics.Color

// Background
val WarmCream = Color(0xFFFFF8F2)

// Primary brand / CTA
val CoralPink = Color(0xFFFF6B8A)

// Accents
val SoftYellow = Color(0xFFFFD166)
val MintGreen = Color(0xFF06C795)
val SkyBlue = Color(0xFF74B9FF)

// Light card backgrounds
val SoftPeach = Color(0xFFFFE4EC)
val SoftLemon = Color(0xFFFFF3C4)
val SoftMint = Color(0xFFCCF5E9)
val SoftSky = Color(0xFFD6ECFF)

// Text
val TextDark = Color(0xFF2D2D2D)
val TextMid = Color(0xFF6B7280)
val TextFaint = Color(0xFF9CA3AF)
val TextOnPrimary = Color(0xFFFFFFFF)

// Surfaces
val SurfaceCard = Color(0xFFFFFFFF)

// States
val ErrorRed = Color(0xFFEF4444)
val SuccessGreen = MintGreen

// Landing gradient
val LandingGrad1 = Color(0xFFFF6B8A)
val LandingGrad2 = Color(0xFFFF9A5C)

// Legacy aliases so existing screens still compile
val ButtonBlue = CoralPink
val TopBarColor = CoralPink
val BackgroundBlue = WarmCream
val SurfaceWhite = SurfaceCard
val TextBlack = TextDark
val TextGrey = TextMid
val TextLight = TextFaint
val TextWhite = TextOnPrimary
val ProgressTrack = SoftPeach
val DividerColor = Color(0xFFEFEFEF)
val LandingStart = LandingGrad1
val LandingEnd = LandingGrad2
